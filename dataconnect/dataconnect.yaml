specVersion: "v1beta"
serviceId: "placement"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "placement-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
